# File Permissions

Understand and modify file permissions:

- Basic permissions: owner, group, others
- Read (r), write (w), execute (x)
- Check permissions: `ls -l`
- Modify permissions: `chmod +rwx filename` or `chmod -rwx filename`
- Octal notation: e.g., `chmod 700 filename`
- Practice with `man chmod` for details
